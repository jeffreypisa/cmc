{% extends "base.twig" %}

{% block content %}
  <div class="content-wrapper">
		<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
      <section class="mod-pageheader d-flex align-items-center" style="background-image: url({{ options.achtergrond404 }}); background-size:cover; background-position: center center;">
        <div class="content w-100">
          <div class="container">
            <div class="row">
              <div class="col-md-10 offset-md-1 text-center">
             		<br><br><br><br><br><br><br><br><br><br><br><br>
              </div>
            </div>
          </div>
        </div>
      </section>
      <section>
        <div class="container">
          <div class="text-center">
            <h1> {{ options.titel404 }} </h1>
            {% if options.tekst404 %}<p class="lead"> {{ options.tekst404 }} </p>{% endif %}
            <br>
            <a href="/" class="btn btn-primary"> <i class="fal fa-arrow-right"></i> {{ options.knoptekst404 }}</a>
          </div>
        </div>
      </section>
		</article>
	</div><!-- /content-wrapper -->
{% endblock %}